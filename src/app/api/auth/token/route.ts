import { NextRequest, NextResponse } from 'next/server'

// Placeholder — full token exchange coming next step
export async function POST(_req: NextRequest) {
  return NextResponse.json({ ok: false, error: 'not_implemented_yet' })
}
